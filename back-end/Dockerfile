# Use official Directus image as base
FROM directus/directus:11.10.2

# Directus runs on port 8055 by default
EXPOSE 8055

# The base image already has proper entrypoint and healthcheck
# Environment variables will be provided via fly.toml or fly secrets
