/**
 * Shared Content
 * ---------------
 * Text used across multiple sections of the site.
 *
 * EDITING GUIDE:
 * - Only change text inside quotation marks ("...")
 * - Do not remove or change the lines starting with 'export'
 * - Save the file after making changes
 */

// Career experience level labels — used on the careers map and in job details
export const levelMap = {
  1: "Entry",
  2: "Mid",
  3: "Late Career",
};

// Fallback prefix when a level number has no label (e.g., shows as "Level 4")
export const levelFallbackPrefix = "Level";
